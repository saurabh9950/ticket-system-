# Secret key used to sign JWTs. Set this to a long random string in production.
# If unset, the app falls back to an insecure default and logs a warning.
JWT_SECRET=change-this-to-a-long-random-string


PORT=8080
